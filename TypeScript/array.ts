const fruits = ["apple", 43, false, "hello"];
let vag: string[] = [
    "hello", 
    "tea",
    "nice",
]

let nums: number[] = [10, 13, 30];

let counter: number = 0;
while(counter < nums.length){
    console.log(nums[counter]);
    counter++;
}