var text = " TypeScript is Awesome ";
text.length; // ➤ Get length
text.trim(); // ➤ Remove spaces
text.toLowerCase(); // ➤ Lowercase
text.toUpperCase(); // ➤ Uppercase
text.includes("Awesome"); // ➤ Check if contains
text.indexOf("is"); // ➤ Find index
text.replace("Awesome", "Cool"); // ➤ Replace text
text.split(" "); // ➤ Split into array
text.slice(0, 10); // ➤ Cut part
text.startsWith("Type"); // ➤ Starts with
